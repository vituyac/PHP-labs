<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Записи к тренерам</title>
    <link rel="stylesheet" href="/css/appointments.css">
</head>
<body>
    <header>
        <h2>Записи к тренерам</h2>
        <a href="/coaches" class="add-button">Назад к списку тренеров</a>
    </header>
    {% if username %}
        <p>Вы вошли как: {{ username }} | <a href="/logout">Выйти</a></p>
    {% endif %}
    <table border="1">
        <tr>
            <th>Имя клиента</th>
            <th>Тренер</th>
            <th>Действия</th>
        </tr>

        {% for appointment in appointments %}
        <tr>
            <td>{{ appointment.client_name }}</td>
            <td>{{ appointment.coach_name }}</td>
            <td>
                <form action="/appointments/delete" method="POST" style="display:inline;" onsubmit="return confirm('Удалить запись клиента {{ appointment.client_name }}?');">
                    <input type="hidden" name="id" value="{{ appointment.id }}">
                    <button type="submit" class="delete-button">Удалить</button>
                </form>
            </td>
        </tr>
        {% endfor %}
    </table>
</body>
</html>
